# =========================================================================
# || GENERAL
# =========================================================================

# Dependency directories
# These folders contain packages and dependencies downloaded by npm or yarn.
# They can be very large and should be regenerated by running `npm install` or `yarn install`.
node_modules/
/backend/node_modules/
.vscode/

# Environment variables
# These files contain sensitive information like API keys, database passwords, etc.
# A `.env.example` file should be committed instead to show what variables are needed.
.env
*.env
backend/.env

# Log files
# Log files are generated during execution and are not needed in the repository.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# =========================================================================
# || EXPO / REACT NATIVE
# =========================================================================

# Expo-specific files and directories
# These are generated by Expo CLI and can be safely ignored.
.expo/
.expo-shared/

# Build and distribution artifacts
# These are generated when you build your app for different platforms.
/web-build/
/dist/
*.apk
*.ipa
*.aab

# Native project directories
# These are generated by `expo prebuild` and should not be committed
# if you are managing your project with Expo's managed workflow.
/android/
/ios/

# Keystores and provisioning profiles
# These contain sensitive signing credentials and should never be committed.
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# =========================================================================
# || BACKEND (Node.js)
# =========================================================================

# Backend build output
# If you have a build step for your backend (e.g., TypeScript), ignore the output directory.
/backend/dist/
/backend/build/

# =========================================================================
# || DEVELOPMENT TOOLS & EDITORS
# =========================================================================

# VS Code specific
# Personal editor settings and workspace history.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains IDEs (WebStorm, IntelliJ)
.idea/

# Vim swap files
*.swp

# =========================================================================
# || OPERATING SYSTEM
# =========================================================================

# macOS
.DS_Store
.AppleDouble
.LSOverride
# Thumbnails
._*
# Trash
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# =========================================================================
# || MISCELLANEOUS
# =========================================================================

# Gemini CLI context file
GEMINI.md
